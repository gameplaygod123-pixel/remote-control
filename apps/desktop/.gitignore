node_modules
dist
out
.DS_Store
.eslintcache
*.log*
*.tsbuildinfo
.verify-tmp
# Staged by build-win.sh from the cached LGPL ffmpeg (~109MB, never committed).
ffmpeg/
# Staged by build-win.sh from native/dxgi-capturer/bin/capturer.exe (Step 3).
capturer/
